// Tzzatziki default template JS

$(document).ready(function() {
	// Do treelist plugin
	$(".treeview").treeview({
		animated: "fast",
		collapsed: true,
		//unique: true,
		persist: "cookie"
	});
});